/**
 * Area interface: settings, the three windows as ApplicationV2, compendium release, messages, welcome window.
 *
 * Server side: tools, resources and prompts of the package.
 */
import type { ServerArea } from '../../tools/areas.js';

export const interfaceArea: ServerArea = {
  id: 'interface',
  tools: [],
  resources: [],
  prompts: [],
};
